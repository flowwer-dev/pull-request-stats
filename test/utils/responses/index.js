const comments = require('./comments.json');
const payload = require('./payload.json');
const pullRequests = require('./pull-requests.json');
const reviews = require('./reviews.json');

module.exports = {
  comments,
  payload,
  pullRequests,
  reviews
};
